// COMPONENTS
import PageWrapper from "components/page-wrapper/PageWrapper.component";

const ReferralPage = () => {
  return <PageWrapper className="">ReferralPage</PageWrapper>;
};

export default ReferralPage;
